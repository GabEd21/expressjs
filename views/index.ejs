<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>

    <% for (let i = 0; i < itemData.length; i++) { %>
        <h3><%= itemData[i].data()['name']%></h3>
        <% for (let j = 0; j < itemData[i].data()['sing'].length; j++) { %>
            <h1><%= itemData[i].data()['sing'][j]%></h1>
        <% } %>
    <% } %>
</body>
</html>
